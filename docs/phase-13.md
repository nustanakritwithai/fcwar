# Phase 13 — Integration stability

Final gates combine syntax checks, direct authority tests, full WebSocket
two-client lifecycle, HTTP asset/UI sanity, deterministic mobile geometry, and
the manual two-tab checklist. Dev diagnostics are opt-in only. The release
keeps placeholder visuals and the strict four-class scope so further work can
extend a measured core rather than replace it.
